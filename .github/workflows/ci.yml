name: CI

on: [push, pull_request]

jobs:
  rust-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2.9.1
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets --locked -- -D warnings
      - run: cargo test --locked

  # Windows native support: the crate builds and the bin unit tests run. The
  # integration tests under tests/ are Unix-only (process groups, real-tool
  # lifecycle, std::os::unix) and don't compile on Windows, so they're excluded
  # by scoping to --bins; Windows-specific unit tests cover the native paths.
  windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2.9.1
      - run: cargo build --locked
      - run: cargo test --locked --bins

  real-tool-tests:
    name: Real tool (${{ matrix.tool }})
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - tool: codex
            package: "@openai/codex@0.141.0"
            cache: codex-0.141.0
            test: real_tool_codex
          - tool: claude
            package: "@anthropic-ai/claude-code@2.1.185"
            cache: claude-2.1.185
            test: real_tool_claude
          - tool: relay
            package: "@anthropic-ai/claude-code@2.1.185"
            cache: relay-claude-2.1.185
            test: test_relay_roundtrip
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - uses: Swatinem/rust-cache@v2.9.1
      - uses: actions/cache@v4
        with:
          path: |
            target/mock-tools
            target/npm-cache
          key: ${{ runner.os }}-mock-tools-${{ matrix.cache }}
      - run: ./scripts/install-mock-tools.sh '${{ matrix.package }}'
      - run: echo "$PWD/target/mock-tools/bin" >> "$GITHUB_PATH"
      - run: cargo test --locked --test '${{ matrix.test }}' -- --ignored --nocapture --test-threads=1
